((.venv) )
USER@DESKTOP-HT764P6 MINGW64 ~/Documents/Projects/Ad-Leak-Engine (master)
$    bash setup_phase14.sh

==========================================================
  Ad-Leak-Engine [SEED: 2399] — PHASE 14 BOOTSTRAP
==========================================================
[DEPLOY] Wrote teardown.md.j2

==========================================================
  [SEED: 2399] PHASE 14 COMPLETE
  Bulletproof L2/L3 Probes & Hyper-Specific Teardown installed.
==========================================================
((.venv) )
USER@DESKTOP-HT764P6 MINGW64 ~/Documents/Projects/Ad-Leak-Engine (master)
$    ./scripts/ale scan shoes US --limit 3
[ALE] Initiating LIVE SCAN for 'shoes' in US...
[ALE] Launching stealth browser and pulling ads...
[ALE] Extracted 30 real ads. Persisted 0 new to SQLite.

[ALE] Auditing: HOKA (1 ads)
[ALE]   L1 Leaks: 2
[ALE]   (L2/L3 crawl skipped: No module named 'ad_leak_engine.leak_scan.self_heal')
[ALE]   Platform: custom (confidence 0.30)
[ALE]   -> Outreach pack saved: output/103032366405927/teardown.md
[ALE]   -> Estimated recovery: $600/month potential recovery

[ALE] Auditing: HOKA (2 ads)
[ALE]   L1 Leaks: 2
[ALE]   (L2/L3 crawl skipped: No module named 'ad_leak_engine.leak_scan.self_heal')
[ALE]   Platform: custom (confidence 0.30)
[ALE]   -> Outreach pack saved: output/966175593248497/teardown.md
[ALE]   -> Estimated recovery: $600/month potential recovery

[ALE] Auditing: American Health Support Community (5 ads)
[ALE]   L1 Leaks: 1
[ALE]   (L2/L3 crawl skipped: No module named 'ad_leak_engine.leak_scan.self_heal')
[ALE]   Platform: custom (confidence 0.30)
[ALE]   -> Outreach pack saved: output/380039845369159/teardown.md
[ALE]   -> Estimated recovery: $300/month potential recovery

[ALE] SCAN COMPLETE. 3 pages audited.
((.venv) )
USER@DESKTOP-HT764P6 MINGW64 ~/Documents/Projects/Ad-Leak-Engine (master)
$    cat output/*/teardown.md
# Ad Infrastructure Teardown: HOKA
**Generated by Ad-Leak-Engine [SEED: 2399]**

## Executive Summary
We analyzed your active Meta ad campaigns and landing page infrastructure.
We identified **2 critical revenue leaks** that are currently degrading your ROAS and inflating your CPA.

---

## Detected Leaks (Ranked by Severity)
### 1. No Ab Variation (Severity: 60%)
- **Tier:** L1
- **Hard Evidence:**
  - `active_ad_count`: 1
- **Business Impact:** Only 1 active ads. Launch variants.
### 2. No Video Creative (Severity: 50%)
- **Tier:** L1
- **Hard Evidence:**
  - `media_types`: ['IMAGE']
- **Business Impact:** No video ads detected. Add video creatives.

---

## Recommended Fixes
### Fix 1: Custom Implementation (Confidence: 30%)
# Custom Fix Guide — Ad-Leak-Engine [SEED: 2399]

## Detected Leak
- **Signal:** `no_ab_variation`
- **Recommendation:** Only 1 active ads. Launch variants.

## What This Fix Does
This fix restores Meta tracking on a custom-built site using either
Google Tag Manager (browser) or a server-side webhook (CAPI).

## Implementation
For browser tracking, use the GTM tag. For server-side reliability,
deploy the CAPI webhook to your backend.

## After Applying
Verify events appear in Meta Events Manager for both browser and
server sources with matching event_ids.


**Expected Outcome:** Server-side Purchase events sent to Meta CAPI with hashed PII.

**Implementation Code:**
*Location: capi_webhook.py (Server-side endpoint (deploy to your backend))*
```python
"""
Meta CAPI Server-Side Webhook - Ad-Leak-Engine [SEED: 2399]
Endpoint: POST /orders/create
Replace: YOUR_PIXEL_ID_HERE, YOUR_ACCESS_TOKEN_HERE
"""
import hashlib
import time

import requests
from fastapi import FastAPI, Request

app = FastAPI()

PIXEL_ID = "YOUR_PIXEL_ID_HERE"
ACCESS_TOKEN = "YOUR_ACCESS_TOKEN_HERE"


def sha256_hash(value: str) -> str:
    return hashlib.sha256(value.strip().lower().encode()).hexdigest()


@app.post("/orders/create")
async def order_created(request: Request):
    order = await request.json()
    event_id = f"order_{order.get('id')}"

    payload = {
        "data": [{
            "event_name": "Purchase",
            "event_time": int(time.time()),
            "event_id": event_id,
            "action_source": "website",
            "user_data": {
                "em": [sha256_hash(order.get("email", ""))],
                "ph": [sha256_hash(order.get("phone", ""))],
            },
            "custom_data": {
                "currency": order.get("currency", "USD"),
                "value": order.get("total", 0),
            },
        }]
    }

    url = f"https://graph.facebook.com/v18.0/{PIXEL_ID}/events"
    resp = requests.post(url, json=payload, params={"access_token": ACCESS_TOKEN})
    return {"status": resp.status_code}

```

**Verification Steps:**
1. Deploy the webhook to your backend
2. Call POST /orders/create on checkout completion
3. Verify the server event appears in Meta Events Manager
### Fix 2: Custom Implementation (Confidence: 30%)
# Custom Fix Guide — Ad-Leak-Engine [SEED: 2399]

## Detected Leak
- **Signal:** `no_video_creative`
- **Recommendation:** No video ads detected. Add video creatives.

## What This Fix Does
This fix restores Meta tracking on a custom-built site using either
Google Tag Manager (browser) or a server-side webhook (CAPI).

## Implementation
For browser tracking, use the GTM tag. For server-side reliability,
deploy the CAPI webhook to your backend.

## After Applying
Verify events appear in Meta Events Manager for both browser and
server sources with matching event_ids.


**Expected Outcome:** Server-side Purchase events sent to Meta CAPI with hashed PII.

**Implementation Code:**
*Location: capi_webhook.py (Server-side endpoint (deploy to your backend))*
```python
"""
Meta CAPI Server-Side Webhook - Ad-Leak-Engine [SEED: 2399]
Endpoint: POST /orders/create
Replace: YOUR_PIXEL_ID_HERE, YOUR_ACCESS_TOKEN_HERE
"""
import hashlib
import time

import requests
from fastapi import FastAPI, Request

app = FastAPI()

PIXEL_ID = "YOUR_PIXEL_ID_HERE"
ACCESS_TOKEN = "YOUR_ACCESS_TOKEN_HERE"


def sha256_hash(value: str) -> str:
    return hashlib.sha256(value.strip().lower().encode()).hexdigest()


@app.post("/orders/create")
async def order_created(request: Request):
    order = await request.json()
    event_id = f"order_{order.get('id')}"

    payload = {
        "data": [{
            "event_name": "Purchase",
            "event_time": int(time.time()),
            "event_id": event_id,
            "action_source": "website",
            "user_data": {
                "em": [sha256_hash(order.get("email", ""))],
                "ph": [sha256_hash(order.get("phone", ""))],
            },
            "custom_data": {
                "currency": order.get("currency", "USD"),
                "value": order.get("total", 0),
            },
        }]
    }

    url = f"https://graph.facebook.com/v18.0/{PIXEL_ID}/events"
    resp = requests.post(url, json=payload, params={"access_token": ACCESS_TOKEN})
    return {"status": resp.status_code}

```

**Verification Steps:**
1. Deploy the webhook to your backend
2. Call POST /orders/create on checkout completion
3. Verify the server event appears in Meta Events Manager
# Ad Infrastructure Teardown: American Health Support Community
**Generated by Ad-Leak-Engine [SEED: 2399]**

## Executive Summary
We analyzed your active Meta ad campaigns and landing page infrastructure.
We identified **1 critical revenue leaks** that are currently degrading your ROAS and inflating your CPA.

---

## Detected Leaks (Ranked by Severity)
### 1. No Video Creative (Severity: 50%)
- **Tier:** L1
- **Hard Evidence:**
  - `media_types`: ['IMAGE']
- **Business Impact:** No video ads detected. Add video creatives.

---

## Recommended Fixes
### Fix 1: Custom Implementation (Confidence: 30%)
# Custom Fix Guide — Ad-Leak-Engine [SEED: 2399]

## Detected Leak
- **Signal:** `no_video_creative`
- **Recommendation:** No video ads detected. Add video creatives.

## What This Fix Does
This fix restores Meta tracking on a custom-built site using either
Google Tag Manager (browser) or a server-side webhook (CAPI).

## Implementation
For browser tracking, use the GTM tag. For server-side reliability,
deploy the CAPI webhook to your backend.

## After Applying
Verify events appear in Meta Events Manager for both browser and
server sources with matching event_ids.


**Expected Outcome:** Server-side Purchase events sent to Meta CAPI with hashed PII.

**Implementation Code:**
*Location: capi_webhook.py (Server-side endpoint (deploy to your backend))*
```python
"""
Meta CAPI Server-Side Webhook - Ad-Leak-Engine [SEED: 2399]
Endpoint: POST /orders/create
Replace: YOUR_PIXEL_ID_HERE, YOUR_ACCESS_TOKEN_HERE
"""
import hashlib
import time

import requests
from fastapi import FastAPI, Request

app = FastAPI()

PIXEL_ID = "YOUR_PIXEL_ID_HERE"
ACCESS_TOKEN = "YOUR_ACCESS_TOKEN_HERE"


def sha256_hash(value: str) -> str:
    return hashlib.sha256(value.strip().lower().encode()).hexdigest()


@app.post("/orders/create")
async def order_created(request: Request):
    order = await request.json()
    event_id = f"order_{order.get('id')}"

    payload = {
        "data": [{
            "event_name": "Purchase",
            "event_time": int(time.time()),
            "event_id": event_id,
            "action_source": "website",
            "user_data": {
                "em": [sha256_hash(order.get("email", ""))],
                "ph": [sha256_hash(order.get("phone", ""))],
            },
            "custom_data": {
                "currency": order.get("currency", "USD"),
                "value": order.get("total", 0),
            },
        }]
    }

    url = f"https://graph.facebook.com/v18.0/{PIXEL_ID}/events"
    resp = requests.post(url, json=payload, params={"access_token": ACCESS_TOKEN})
    return {"status": resp.status_code}

```

**Verification Steps:**
1. Deploy the webhook to your backend
2. Call POST /orders/create on checkout completion
3. Verify the server event appears in Meta Events Manager
# Ad Infrastructure Teardown: HOKA
**Generated by Ad-Leak-Engine [SEED: 2399]**

## Executive Summary
We analyzed your active Meta ad campaigns and landing page infrastructure.
We identified **2 critical revenue leaks** that are currently degrading your ROAS and inflating your CPA.

---

## Detected Leaks (Ranked by Severity)
### 1. No Ab Variation (Severity: 60%)
- **Tier:** L1
- **Hard Evidence:**
  - `active_ad_count`: 2
- **Business Impact:** Only 2 active ads. Launch variants.
### 2. No Video Creative (Severity: 50%)
- **Tier:** L1
- **Hard Evidence:**
  - `media_types`: ['IMAGE']
- **Business Impact:** No video ads detected. Add video creatives.

---

## Recommended Fixes
### Fix 1: Custom Implementation (Confidence: 30%)
# Custom Fix Guide — Ad-Leak-Engine [SEED: 2399]

## Detected Leak
- **Signal:** `no_ab_variation`
- **Recommendation:** Only 2 active ads. Launch variants.

## What This Fix Does
This fix restores Meta tracking on a custom-built site using either
Google Tag Manager (browser) or a server-side webhook (CAPI).

## Implementation
For browser tracking, use the GTM tag. For server-side reliability,
deploy the CAPI webhook to your backend.

## After Applying
Verify events appear in Meta Events Manager for both browser and
server sources with matching event_ids.


**Expected Outcome:** Server-side Purchase events sent to Meta CAPI with hashed PII.

**Implementation Code:**
*Location: capi_webhook.py (Server-side endpoint (deploy to your backend))*
```python
"""
Meta CAPI Server-Side Webhook - Ad-Leak-Engine [SEED: 2399]
Endpoint: POST /orders/create
Replace: YOUR_PIXEL_ID_HERE, YOUR_ACCESS_TOKEN_HERE
"""
import hashlib
import time

import requests
from fastapi import FastAPI, Request

app = FastAPI()

PIXEL_ID = "YOUR_PIXEL_ID_HERE"
ACCESS_TOKEN = "YOUR_ACCESS_TOKEN_HERE"


def sha256_hash(value: str) -> str:
    return hashlib.sha256(value.strip().lower().encode()).hexdigest()


@app.post("/orders/create")
async def order_created(request: Request):
    order = await request.json()
    event_id = f"order_{order.get('id')}"

    payload = {
        "data": [{
            "event_name": "Purchase",
            "event_time": int(time.time()),
            "event_id": event_id,
            "action_source": "website",
            "user_data": {
                "em": [sha256_hash(order.get("email", ""))],
                "ph": [sha256_hash(order.get("phone", ""))],
            },
            "custom_data": {
                "currency": order.get("currency", "USD"),
                "value": order.get("total", 0),
            },
        }]
    }

    url = f"https://graph.facebook.com/v18.0/{PIXEL_ID}/events"
    resp = requests.post(url, json=payload, params={"access_token": ACCESS_TOKEN})
    return {"status": resp.status_code}

```

**Verification Steps:**
1. Deploy the webhook to your backend
2. Call POST /orders/create on checkout completion
3. Verify the server event appears in Meta Events Manager
### Fix 2: Custom Implementation (Confidence: 30%)
# Custom Fix Guide — Ad-Leak-Engine [SEED: 2399]

## Detected Leak
- **Signal:** `no_video_creative`
- **Recommendation:** No video ads detected. Add video creatives.

## What This Fix Does
This fix restores Meta tracking on a custom-built site using either
Google Tag Manager (browser) or a server-side webhook (CAPI).

## Implementation
For browser tracking, use the GTM tag. For server-side reliability,
deploy the CAPI webhook to your backend.

## After Applying
Verify events appear in Meta Events Manager for both browser and
server sources with matching event_ids.


**Expected Outcome:** Server-side Purchase events sent to Meta CAPI with hashed PII.

**Implementation Code:**
*Location: capi_webhook.py (Server-side endpoint (deploy to your backend))*
```python
"""
Meta CAPI Server-Side Webhook - Ad-Leak-Engine [SEED: 2399]
Endpoint: POST /orders/create
Replace: YOUR_PIXEL_ID_HERE, YOUR_ACCESS_TOKEN_HERE
"""
import hashlib
import time

import requests
from fastapi import FastAPI, Request

app = FastAPI()

PIXEL_ID = "YOUR_PIXEL_ID_HERE"
ACCESS_TOKEN = "YOUR_ACCESS_TOKEN_HERE"


def sha256_hash(value: str) -> str:
    return hashlib.sha256(value.strip().lower().encode()).hexdigest()


@app.post("/orders/create")
async def order_created(request: Request):
    order = await request.json()
    event_id = f"order_{order.get('id')}"

    payload = {
        "data": [{
            "event_name": "Purchase",
            "event_time": int(time.time()),
            "event_id": event_id,
            "action_source": "website",
            "user_data": {
                "em": [sha256_hash(order.get("email", ""))],
                "ph": [sha256_hash(order.get("phone", ""))],
            },
            "custom_data": {
                "currency": order.get("currency", "USD"),
                "value": order.get("total", 0),
            },
        }]
    }

    url = f"https://graph.facebook.com/v18.0/{PIXEL_ID}/events"
    resp = requests.post(url, json=payload, params={"access_token": ACCESS_TOKEN})
    return {"status": resp.status_code}

```

**Verification Steps:**
1. Deploy the webhook to your backend
2. Call POST /orders/create on checkout completion
3. Verify the server event appears in Meta Events Manager
((.venv) )
USER@DESKTOP-HT764P6 MINGW64 ~/Documents/Projects/Ad-Leak-Engine (master)
$
