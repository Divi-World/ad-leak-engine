[build-system]
requires = ["setuptools>=68.0", "wheel"]
build-backend = "setuptools.backends._legacy:_Backend"

[project]
name = "ad-leak-engine"
version = "0.1.0"
description = "World's #1 automated ad infrastructure audit system"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.110.0",
    "uvicorn[standard]>=0.29.0",
    "sqlmodel>=0.0.16",
    "pydantic>=2.6.4",
    "pydantic-settings>=2.2.1",
    "curl-cffi>=0.6.2",
    "playwright>=1.42.0",
    "camoufox[geoip]>=0.4.0",
    "jinja2>=3.1.3",
    "httpx>=0.27.0",
    "diskcache>=5.6.3",
    "beautifulsoup4>=4.12.3",
    "lxml>=5.1.0",
    "python-dotenv>=1.0.1",
]

[project.optional-dependencies]
test = [
    "pytest>=8.0.0",
    "pytest-cov>=4.1.0",
    "pytest-asyncio>=0.23.0",
]
scrape = [
    "camoufox[geoip]>=0.4.0",
    "curl-cffi>=0.6.2",
    "playwright>=1.42.0",
]

[tool.setuptools.packages.find]
where = ["src"]
